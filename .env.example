# Django Settings
# SECRET_KEY: Django secret key for cryptographic signing
# Generate a new secret key: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
# Default is insecure and only for development
SECRET_KEY=django-insecure-change-this-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database Configuration
# DATABASE: Set to 'postgres' to use PostgreSQL, or leave empty/default to use SQLite (no configuration needed)
# For SQLite (default): Leave DATABASE empty or set to 'sqlite' - no secrets required
# For PostgreSQL (Docker/production): Set DATABASE=postgres and configure the following
DATABASE=
# DATABASE=postgres
# POSTGRES_DB=rxinox
# POSTGRES_USER=rxinox
# POSTGRES_PASSWORD=rxinox
# SQL_HOST=db
# SQL_PORT=5432

# Email Configuration (optional, for future order email functionality)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-email-password
# MANAGER_EMAIL=manager@rxinox.com
